# Quant Engine v20 — runtime dependencies
# Install:  pip install -r requirements.txt
aiohttp>=3.9
aiosqlite>=0.19
flask>=2.2
python-dotenv>=1.0

# --- Optional -------------------------------------------------------------
# ccxt adds the redundant public candle fallback chain (Bybit/OKX/Binance).
# The engine works with AriaX-only candles if ccxt is not installed.
ccxt>=4.0
